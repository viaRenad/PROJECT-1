import 'package:flutter/material.dart';

class JourneyHome extends StatelessWidget {
  const JourneyHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("This is the active journey home.")),
    );
  }
}
